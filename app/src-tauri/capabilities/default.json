{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window: core defaults only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
